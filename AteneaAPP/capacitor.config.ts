import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hotelgest.companionhg',
  appName: 'AteneaAPP',
  webDir: 'build',
  bundledWebRuntime: false,

};

export default config;
